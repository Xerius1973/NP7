const http=require('http'),fs=require('fs'),path=require('path');const port=process.env.PORT||10000;const root=path.join(__dirname,'public');const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.webmanifest':'application/manifest+json; charset=utf-8'};http.createServer((req,res)=>{let u=new URL(req.url,'http://x').pathname;if(u==='/')u='/index.html';let f=path.join(root,path.normalize(u).replace(/^[/\\]+/,''));fs.readFile(f,(e,d)=>{if(e){res.writeHead(404);return res.end('NP7: fichier introuvable')}res.writeHead(200,{'Content-Type':mime[path.extname(f)]||'application/octet-stream','Cache-Control':'no-cache'});res.end(d)})}).listen(port,'0.0.0.0',()=>console.log(`NP7 sans OpenAI lance sur 0.0.0.0:${port}`));
